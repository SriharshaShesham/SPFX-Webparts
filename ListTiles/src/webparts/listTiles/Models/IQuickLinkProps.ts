export default interface IQuickLinkProps {
    
        title: string;
        icon: string;
        tooltip: string;
        flag: string;
        sort: string;
        hyperlink: string;
        id:number;
        displayType:number;
}
